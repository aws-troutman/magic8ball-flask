version: 0.0
os: linux
files:
  - source: /
    destination: /opt
hooks:
  AfterInstall:
    - location: scripts/install_dependencies
      runas: root
    - location: scripts/start_server
      timeout: 300
      runas: root
  ApplicationStop:
    - location: scripts/stop_server
      timeout: 300
      runas: root
